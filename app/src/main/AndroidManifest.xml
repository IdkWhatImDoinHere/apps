<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gomeds">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoMeds"
        tools:replace="android:theme">
        <activity android:name=".Delivery_Registration"></activity>
        <activity android:name=".Delivery_Loginphone" />
        <activity android:name=".Delivery_Login" />
        <activity android:name=".Registration" />
        <activity android:name=".Loginphone" />
        <activity android:name=".Login" />
        <activity android:name=".PharRegistration" />
        <activity android:name=".Pharloginphone" />
        <activity android:name=".Pharlogin" />
        <activity android:name=".ChooseOne" />


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />-->
                <category android:name="android.intent.category.LAUNCHER" />-->
            </intent-filter>
        </activity>

                <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>